import './App.css';

function App() {
  return (
    <div className="App">
      <h1>Test</h1>
    </div>
  );
}

// Default export is used to export a single class, function or primitive from a script file.
// Named export is used to export several values from a script file.
// e.g. export { myFunction }; or export const foo = Math.sqrt(2);
export default App;
